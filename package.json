{
  "name": "gotrip",
  "version": "1.0.0",
  "description": "School project for a trip planner",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "bk": "npm run dev --prefix backend",
    "cl": "npm run dev --prefix frontend",
    "build": "npm i --prefix backend && npm run build --prefix backend && npm i --prefix frontend && npm run build --prefix frontend",
    "start": "cd backend && npx cross-env NODE_ENV=production && npm run start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
