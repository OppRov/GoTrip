import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#F1671A",
    },
    secondary: {
      main: "#696969",
    },
    tertiary: {
      main: "#222222",
    },
    quaternary: {
      main: "#FFFFFF",
    },
  },
});
